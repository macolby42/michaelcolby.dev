name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Setting environment variables
      env:
        EMAIL: macolby42@students.tnech.edu 
    - name: Installing packages
      run: yarn
    - name: Building and deploying
      run: yarn deploy
